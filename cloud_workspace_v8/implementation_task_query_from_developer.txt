T1 — Capture real learner-conversation logs (P0, blocks all neural upgrades)



1.Identification: student_id_hash (an anonymized ID) and session_id. already UI has these things right or is it need to be implemented?

2. How to provide these: Immediate Results (short_term_outcome): Did the student get the next answer right? Was their confusion reduced? Did they use a hint?


3.whether the below task is done or yet to be done?

"tutor_loop.py: Update this to include the new data fields when a turn is logged.

collect_delayed.py: [NEW] Create this to handle the 3/7/21-day re-testing schedule.

learner_state.py: Update this to manage the individual state/timing for each student's delayed tests.

device_config.py: Update data directory paths if necessary."

4.is this having perfect schema, if not what needs to be changed? rag_store/learning_log.jsonl


T2 — Add the acknowledgment label and re-curate (P1)

1. what are the existing markers in cues.py (like "because", "but", "since"), isn't it making regrex based. how to fix this solution in generalization

2. do we need to separate the confusion and acknowledgement separate and remove multiple confusion labels from the datatset what is present now?

3. do we need to create file like curate_dataset.py using which the new dataset will be generated?

4. if we are using example like, "i understood but why x=2?" whether the data take x=2 as an general or specifically it will fire for only x=2, we need generalization here.

T4 — Normalize off-action-space policy tags

1. isn't it wrong to do this

"Multi-actions: If a row tries to do two things at once, just keep the first listed action and throw the rest away.

Renaming: If a row says VERBAL_ANALOGY, overwrite it with VISUAL_ANALOGY.

Dropping: If a row lists RESUME_STATE or REQUEST_HINT, drop those actions entirely.

Audit Trail: Once your code rewrites the dataset, save it as a new file and "log the diff" (print out exactly what changed so other developers can verify it worked)."

2. isn't it right to add new action in case it geneuinely falls under different action which is not listed in the 15 actions.



T5 — Scale the seed-level derived datasets

1. how to perform these things while collecting data, as students will be learning, where to provide these label or how to inform there was leak?

"Every time the system generates a response alongside a background manifest, that pairing can be saved as a training example for the grounding_guard dataset.

Every time the system gives a user a hint, you can log whether it gave away too much information (a "leak") or not, giving you perfect training data for your clue bank. "

2. will there be two tags or one because hard negative and near miss are two different things, how many samples required for both.


T6 — Replace HOPE rater B with human-teacher answer labels 

i think in this we have currently dropped prompts with rewrite/drop. check actually in the code what has happened, in the report it might not have been updated clearly, check what is pending in this.

T7 — Retrain HOPE detectors + fix absolute calibration

1. basically in this we have to retrain the HOPE metrics
2. but how to fix Relative vs. Absolute Scoring.
3. how to distinguish between a genuinely "strong" answer and one that was just "memorized".