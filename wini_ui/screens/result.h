/* result — end-of-stage score (spec §Result). Shared frame + the result card and
 * a gentle way home. No pass/fail language. */
#ifndef WINI_SCREENS_RESULT_H
#define WINI_SCREENS_RESULT_H

#include "lvgl/lvgl.h"

lv_obj_t *wini_screen_result_create(lv_obj_t *parent);

#endif /* WINI_SCREENS_RESULT_H */
